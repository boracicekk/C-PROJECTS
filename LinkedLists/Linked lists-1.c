#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
struct student{
	int stdntno;
	char name[50];
	int midtermgrade;
	int finalgrade;
	};
	int main(){
	 *head,*p;
	printf("How much student we have?"); scanf("%d",&N);
	for(i=0;i<N;i++){
	printf("Enter new student number:"); scanf("%d",&newNode->stdntno);
	printf("Enter new name:"); scanf("%s",&newNode->name);
	printf("Enter new name:"); scanf("%d",&newNode->midtermgrade);
	printf("Enter new name:"); scanf("%d",&newNode->finalgrade);
	}
	p->next=NULL;
	
	return head;
}
