#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a=5, b=10,gecici;
	gecici=a;
	a=b;
	b=gecici;
	printf("A=%d, B=%d, gecici= %d", a,b);
	return 0;
}
gecici(x,y)

