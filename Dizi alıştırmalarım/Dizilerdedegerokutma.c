#include <stdio.h>
#include <stdlib.h>
#define SIZE 5

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int i;
	double a[SIZE];
	printf("%d defa deger giriniz:\n", SIZE);
	for (i=0;i<SIZE;i++)
	{
		scanf("%1f", &a[SIZE]);
	}
	return 0;
}
